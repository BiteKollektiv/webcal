  <%= link_to 'Back', :back %>
<p>
  <b><%= t('events.title') %></b>
  <%= @event.title %>
</p>
<p>
  <b><%= t('events.description') %></b>
  <%= @event.description %>
</p>
<p>
  <b><%= t('events.location') %></b>
  <%= @event.location %>
</p>

<p>
  <b><%= t('events.starts_at') %></b>
  <%= @event.starts_at %>
</p>

<p>
  <b><%= t('events.ends_at') %></b>
  <%= @event.ends_at %>
</p>

<%= link_to t('events.edit'), edit_calendar_event_path(@calendar.token_write, @event.id) %>